<Window x:Class="ProcessCalc.Ventanas.Configuraciones.ConfigurarOperaciones_Elemento"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProcessCalc.Ventanas.Configuraciones"
        mc:Ignorable="d"
        Title="Configurar las operaciones de cadenas de texto del elemento" Height="450" Width="800" Loaded="Window_Loaded" WindowStartupLocation="CenterScreen" SizeToContent="WidthAndHeight">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock x:Name="textoModoOperacion" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10">Estas operaciones de cadenas de texto se ejecutarán en el orden establecido:</TextBlock>
        <TextBlock x:Name="textoModoSeleccion" Visibility="Collapsed" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="10">Estas selecciones de cadenas de texto se utilizarán para clasificar las cantidades en el clasificador:</TextBlock>

        <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Button x:Name="Agregar" Grid.Column="0" Margin="10" Padding="10" MaxWidth="250" HorizontalAlignment="Left" Click="Agregar_Click">Agregar operación</Button>
            <Button x:Name="AgregarModoSeleccion" Visibility="Collapsed" Grid.Column="0" Margin="10" Padding="10" MaxWidth="250" HorizontalAlignment="Left" Click="Agregar_Click">Agregar selección</Button>
            <Button x:Name="Quitar" Grid.Column="1" Margin="10" Padding="10" MaxWidth="250" HorizontalAlignment="Left" Click="Quitar_Click" >Quitar operación</Button>
            <Button x:Name="QuitarModoSeleccion" Visibility="Collapsed" Grid.Column="1" Margin="10" Padding="10" MaxWidth="250" HorizontalAlignment="Left" Click="Quitar_Click" >Quitar selección</Button>
            <Button x:Name="moverCondicionAIzquierda" Grid.Row="0" Grid.Column="2" Margin="10" Click="moverAIzquierda_Click" >Subir operación</Button>
            <Button x:Name="moverCondicionADerecha" Grid.Row="0" Grid.Column="3" Margin="10" Click="moverADerecha_Click" >Bajar operación</Button>
        </Grid>

        <ScrollViewer Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2">
            <StackPanel x:Name="operaciones" Orientation="Vertical"></StackPanel>
        </ScrollViewer>

        <Button x:Name="Cancelar" Grid.Column="0" Grid.Row="4" Margin="10" Padding="10" MaxWidth="100" Click="Cancelar_Click">Cancelar</Button>
        <Button x:Name="Guardar" Grid.Column="1" Grid.Row="4" Margin="10" Padding="10" MaxWidth="100" Click="Guardar_Click">Guardar</Button>
    </Grid>
</Window>
